import Hero from '../components/Hero';

const HomePage = () => {
    return (
        <div>
            <main>
                <section>
                    <Hero />
                </section>
            </main>
        </div>

    );
};

export default HomePage;

